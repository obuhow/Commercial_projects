[tool.poetry]
name = "sweetapp"
version = "0.1.0"
description = ""
authors = ["obuhow <p@obuhov.pro>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
